name: Windows Build

on:
  workflow_dispatch:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Windows Server 2025 and Visual Studio 2026
    runs-on: windows-2025-vs2026
    timeout-minutes: 180

    steps:
      - name: Check out diff-solver
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Check out Spectra SDK
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: Xayah-Graphics/spectra
          path: spectra-source
          persist-credentials: false

      - name: Install CMake and Ninja
        run: python -m pip install cmake==4.4.0 ninja

      - name: Install CUDA
        shell: pwsh
        run: |
          $installer = Join-Path $env:RUNNER_TEMP 'cuda-13.3-installer.exe'
          $cudaPath = 'C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.3'
          Invoke-WebRequest -Uri 'https://developer.download.nvidia.com/compute/cuda/13.3.0/network_installers/cuda_13.3.0_windows_network.exe' -OutFile $installer
          "CUDA_PATH=$cudaPath" >> $env:GITHUB_ENV
          "$cudaPath\bin" >> $env:GITHUB_PATH
          $process = Start-Process -FilePath $installer -ArgumentList '-s' -Wait -PassThru
          exit $process.ExitCode

      - name: Enter Visual Studio developer environment
        shell: pwsh
        run: |
          $installation = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          $developerCommand = Join-Path $installation 'Common7\Tools\VsDevCmd.bat'
          cmd /d /s /c "`"$developerCommand`" -arch=x64 -host_arch=x64 && set" | ForEach-Object {
              if ($_ -match '^([^=]+)=(.*)$') { "$($matches[1])=$($matches[2])" >> $env:GITHUB_ENV }
          }

      - name: Build and install Spectra SDK
        shell: pwsh
        run: |
          $prefix = Join-Path $env:RUNNER_TEMP 'SpectraSDK'
          cmake -S spectra-source/sdk -B spectra-sdk-cmake-build-release -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_INSTALL_PREFIX="$prefix"
          cmake --build spectra-sdk-cmake-build-release --parallel 30
          cmake --install spectra-sdk-cmake-build-release --component SDK

      - name: Configure diff-solver
        shell: pwsh
        run: |
          $prefix = Join-Path $env:RUNNER_TEMP 'SpectraSDK'
          cmake -S . -B cmake-build-release -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_PREFIX_PATH="$prefix" `
            -DDIFF_SOLVER_BUILD_EXAMPLES=ON `
            -DDIFF_SOLVER_BUILD_SPECTRA_PROVIDERS=ON

      - name: Build diff-solver
        run: cmake --build cmake-build-release --parallel 30
